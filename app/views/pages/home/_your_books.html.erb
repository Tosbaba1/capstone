<section class="sessions-home__panel sessions-home__panel--quiet sessions-home__section sessions-home__section--books">
  <header class="sessions-home__panel-head sessions-home__panel-head--stack">
    <h2 class="sessions-home__section-title mb-0">Your books</h2>
  </header>

  <% if readings.any? %>
    <div class="sessions-home__book-row" aria-label="Your books">
      <% readings.each do |reading| %>
        <%= render "pages/home/book_item", reading: reading %>
      <% end %>
    </div>
  <% else %>
    <div class="sessions-home__empty-state sessions-home__empty-state--inline">
      <p class="mb-0">Add a book to keep it close.</p>
    </div>
  <% end %>
</section>
