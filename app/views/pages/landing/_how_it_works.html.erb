<section class="landing-section" aria-labelledby="landing-how-it-works">
  <div class="landing-section__header">
    <p class="page-eyebrow mb-2">How it works</p>
    <h2 id="landing-how-it-works" class="landing-section__title mb-0">Three small steps into a calmer reading habit.</h2>
  </div>

  <div class="landing-steps">
    <% landing_how_it_works_steps.each do |step| %>
      <article class="landing-step content-box">
        <p class="landing-step__number mb-2"><%= step[:number] %></p>
        <h3 class="landing-step__title mb-2"><%= step[:title] %></h3>
        <p class="landing-step__copy mb-0"><%= step[:copy] %></p>
      </article>
    <% end %>
  </div>
</section>
